const sayHi = (name) => {
    console.log(`Hello my friends ${name}`);
}

module.exports = sayHi


// const names = require('./modules/names');
// const sayHi = require('./modules/sayHi');
// require('./modules/selfFunction');

// sayHi(names.name1)
// sayHi(names.name2)
// sayHi(names.name3)